/** ### CREATE OBJECT WITH PROPERTIES ### */

/**
 * Rules of creating objects:
 * - object should be wrapped by {}
 * - key on the left side,
 * - `:` is required between the key and the value
 * - `,` should be uded to separate key-value pairs
 */

const product = {
    name: "Laptop",
    brand: "Lenovo",
    price: 12000,
    discount: 3.5,
    isActive: true
}
console.log(product);